from setuptools import find_packages, setup

setup(
    name='Rosmaster_Lib',
    version='1.7.3',
    author='Yahboom Team',
    packages=find_packages(),
)

# cd py_install
# sudo python3 setup.py install
